# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: destiny-chart-insight
metadata:
  template: destiny-chart-insight@0.0.1-beta

services:
  web:
    project: .
    language: js
    host: appservice
    hooks:
      predeploy:
        shell: sh
        run: npm run build
        continueOnError: false
